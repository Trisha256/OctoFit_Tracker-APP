# djongo 1.3.6 declares a strict dependency on sqlparse==0.2.4 which has known
# security vulnerabilities (denial-of-service from heavily nested lists).
# However, djongo is functionally compatible with the patched sqlparse>=0.5.0.
# This file must be installed with --no-deps to bypass the overly strict constraint:
#
#   pip install --no-deps -r requirements-nodeps.txt
#   pip install -r requirements.txt
#
djongo==1.3.6
