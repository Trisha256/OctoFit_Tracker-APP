[build]
  base = "octofit-tracker/frontend"
  publish = "octofit-tracker/frontend/build"
  command = "npm run build"

# Set REACT_APP_API_URL to your deployed backend URL in Netlify environment settings
# e.g. REACT_APP_API_URL = "https://your-backend-api.example.com"
